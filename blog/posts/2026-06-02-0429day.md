---
title: "4.29日报(ai)"
date: 2026-06-02
tags:
  - html
  - table
  - rowspan
  - colspan
  - web-development
summary: "本文介绍了HTML中的基础标签，包括段落标签、不换行标签、超链接标签和图片标签。重点讲解了表格标签的使用，特别是rowspan（行合并）和colspan（列合并）属性的用法。通过示例说明如何在td上应用这两个属性来实现单元格的合并（合并2行3列，共6个单元格）。同时，文章还明确了tr代表表格的行、td代表表格的列，为初学者提供了清晰的HTML表格基础知识点。"
---

### 4.29

### HTML基础标签

- `<p></p>` 段落，自动换行
- `<span></span>` 不换行
- `<a href=""></a>` 超链接
- `<img src="" />` 图片

#### `<table>` 标签的使用

主要：

`rowspan`（行合并）与 `colspan`（列合并）的用法

一般都写在 `<td>` 上。例如：`<td rowspan="2" colspan="3">` 从该单元格开始合并 2 行 3 列，共计 6 个单元格。

- `rowspan`：从该单元格开始合并几行，默认一列
- `colspan`：从该单元格开始合并几列，默认一行

- `<tr>` 代表表格的行
- `<td>` 代表表格的列